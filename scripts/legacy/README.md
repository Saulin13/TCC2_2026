# Scripts legados

Scripts movidos para cá **não fazem parte** da pipeline oficial (`00_run_pipeline.py`).
Foram preservados apenas como referência histórica.

| Script | Motivo |
|--------|--------|
| `17_classificar_cobertura.py` | Substituído por `14_classify_coverage.py` (suporte a `--dataset`, saídas com sufixo por dataset, sem gráfico duplicado na pipeline). |
